# UI 컴포넌트 규칙 (General)

## 목표
- UI 일관성 유지
- 재사용성 확보
- 접근성 준수

## 기본 원칙
- 반복되는 UI 패턴은 컴포넌트로 추출
- 스타일은 디자인 토큰(테마) 기반으로만 정의
- 사용자 피드백(성공/실패/진행중)은 토스트/스낵바 등으로 통일 (브라우저 alert 지양)

## Props/Interface
- 컴포넌트 Props는 타입으로 명시
- optional은 `?`, 기본값은 명시

## 반응형
- 모바일 우선
- 브레이크포인트/레이아웃 규칙을 문서화
